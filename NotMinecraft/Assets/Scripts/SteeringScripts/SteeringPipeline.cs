using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SteeringPipeline : MonoBehaviour
{
    //List<Targeter> targeters = new(); //For determining the target action/goal
    //List<Decomposer> decomposers = new(); //For determining what sub goal in the path to go to
    //List<Constraint> constraints = new(); //For determining if a movement is invalid and fix movment
    //List<Actuator> actuators = new(); //For determining the actual movement of the character

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
